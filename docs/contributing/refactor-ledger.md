# Refactor ledger

The memory between passes of the [refactor program](./refactor-program.md). An agent starting a pass reads this file first and appends to it last. Rows are never rewritten except to change a status; a superseded row keeps its text and gains a pointer to what superseded it. Every number here is reproducible by the command beside it, and the baseline is refreshed only by a pass row that names the new SHA.

## Program baseline

Taken at `73199f48c` (2026-09-03) with `cargo xtask atlas survey --out /tmp/atlas/survey.md`.

| measure | value |
| --- | ---: |
| production SLOC (`crates/rimz/src`) | 213086 |
| test SLOC in source | 177938 |
| escaping items | 5386 |
| `cx` (over-threshold excess, summed) | 472.6 |
| scoped commits in history | 3219 |
| admitted upward sites out of `store` | 185 |
| module cycles | 31 |

## Seam queue

Ordered. A seam pass at the head is proposed before any module pass. Status is `queued`, `in flight <branch>`, `landed <pass>`, or `rejected <reason>`. These rows are survey findings awaiting a review, not decisions; the review may reject one.

| # | seam | evidence at baseline | status |
| --- | --- | --- | --- |
| 1 | `store` → `agents` and `store` → `message` upward dependencies; decide the direction and close it | `debt`: store admits agents 134 sites, message 22; cycles agents ↔ store 46/134, message ↔ store 24/22, both cross-layer | landed pass-1 |
| 2 | Adapter sibling families in `agents/adapters/*`: collapse each onto one implementation with the divergences a fix pins | `shapes`: decode-hook family 9 members / 8 siblings (`*/mod.rs`, 935 SLOC in play); `spend.rs` cache 4 siblings (354); `ask.rs` plan 3 siblings (182); `install.rs` 2 siblings (90) | landed pass-2 |
| 3 | `mux` ↔ `sidebar` and `daemon_view` ↔ `mux` cycles | cycles mux ↔ sidebar 27/42 cross-layer; daemon_view ↔ mux 10/3 same layer; mux admits sidebar 27 sites | `daemon_view` ↔ `mux` landed pass-3; `mux` ↔ `sidebar` landed pass-4 |
| 4 | Homes for `store::workspace_record` (→ `workspace`) and `store::snapshot::compose_channel` (`transcript`, `harness::target` callers); the only upward L2 → store sites after pass 1 | pass-1 re-layer admissions: `workspace` 3 sites, `transcript` 1, `pane` 1 | landed pass-3 |
| 5 | `agents` → `mux` (`NamedKey`, `CommandSpec`), `daemon_view` (markers), `diag::rotating`, `observability`: adapter-side reach into peers exposed by the re-layer | pass-1 re-layer admissions: `mux` 6 sites, `daemon_view` 3, `diag::rotating` 3; no current `observability` site | landed pass-3 |
| 6 | `wakeup` below `store`: the wire sits in the `mux` layer, so the store reaches up to send | pass-4 leftovers: `store` → `wakeup` 2 production sites (`store/writer/publish.rs:179`, `store/gc/collect.rs:12`); the wire holds the two references that keep it there, `mux::ClientPaneView` (`wakeup/events.rs:66`) and `mux::focus_anchor::FocusNonce` (`wakeup/events.rs:73`) | landed pass-5 |
| 7 | `store` → `harness` upward dependencies (signal vocabulary, petname, prompt alignment, run schema, run wake sender): close the direction | pass-6 admission intent `store → harness::schedule::signal` close: later seam; survey at `97a947974`: 17 sites into harness (run 6, signal 5, petname 4, run_wake 1, target 1), cycle harness ↔ store 47/17 cross-layer | landed pass-7 |
| 8 | `store` → `remote::link` and `store` → `disk_usage` remaining non-diag upward dependencies and the four small upward edges: `harness` → `sidebar::refresh::pr`, `osc` → `config`/`mux`, `diag` → `sidebar::presence`, `build_id` → `proc` | survey at `6d13498b3`: store upward sites 6 = diag 4, disk_usage 1, remote::link 1; worktree → disk_usage 1 | landed pass-8 |
| 9 | `message` → `harness::target` agent-address grammar, renderer, pane binding and launch lineage | pass-13 admission: 17 production sites; canonical addressing now lives in L4 `address` | landed pass-14 |
| 10 | `diag` re-layer to L3 below `store` (`LinkTier` moves down with it), closing the `store ↔ diag::record` cycle from the diag side | pass-15a: store → diag 4 sites, store → diag::record 3; a store-side close measured +65 SLOC | landed pass-16 |

The queue closed after pass 8; pass 13 reopened it for seam 9, which landed in pass 14. Pass 15a reopens the queue for seam 10 as a finding contradicting pass 8, not an approved re-layer. Pass 16 landed seam 10 on the user's reopen.

## Pass targets

The direction each pass wrote, kept so a later pass can quote it rather than re-derive it. A target that a later pass supersedes keeps its text and gains a pointer.

### Pass 2 family verdicts

| family | members reviewed | verdict and pins |
| --- | --- | --- |
| hook decode | every built-in adapter and the process-plugin adapter (`Kiro` uses the default) | holds: the generic caller already sees one `HookOutput`; event choreography is provider policy. Fix pins: `929ba4745`, `75ff1da53`, `53cc53892`, `6d79cac0c`, `55685b741`, `7c42e0184`, `f93cecb04`, `491ecd20d`, `9acad2888`, `567a18bc8`, `940758fad`, `071bf1ba3`. |
| spend cache | the ten adapter `spend.rs` parsers | holds: `agents::spending` already owns pricing, cache entries, deduplication, and aggregation; providers retain format, replay, rewind, and cost precedence. Fix pins: `b23d1898f`, `5951e11bd`, `2d2bab7c3`, `3b1754c89`, `517de4f21`, `217577bcd`, `253cac4c9`, `6e0a6f814`, `46265e8b2`. |
| lifecycle tool classification | Claude, Codex, Qwen | holds: the classifiers live once; event tables remain provider policy. The hook-decode fixes above pin the divergent arms. |
| answer plan | Claude, Codex, Pi | holds: extraction is shared; key sequences implement different provider TUIs. Fix pins: `d3d072eab`, `bff26f369`, `0908a9a52`, `512cb7390`. |
| paired settings install | Copilot, Cursor | holds: `settings_json::commit_pair` is the shared transaction; cleanup rollback differs. Fix pins: `a84ad352b`, `0c5284908`. |
| `result.kind() != ValueRefreshKind::Cached` | six test-only sites in Antigravity, Claude, Codex, Kiro | holds: counters instrument the one production `StableValueCache` policy; `23ddf5697` pins Kiro's distinct invalidation branch. |

Module-pass notes: Copilot and Cursor still hand-build parallel `report_files` lists (`copilot/install.rs`, `cursor/install.rs`); Droid and each sibling retain provider-specific session-origin stamping. The store-free `spend.rs` invariant matches by filename, so a future shared spend helper outside a `spend.rs` needs the invariant widened with it.

### Pass 4 target

The direction the pass writes into `crates/rimz/src/mux/AGENTS.md` and `crates/rimz/src/sidebar/AGENTS.md`: `mux` never imports `sidebar`. The sidebar's wire (heartbeat record, event vocabulary, datagram send) is the leaf module `wakeup`, beside `mux` in the layering and above `store`, reached alike by `mux`, `store`, `remote_control`, and `sidebar`. Every runtime file the multiplexer writes or dispatches on (focus anchor, width target, pane topology, presence-desired) is owned by `mux`. A bound on another module's behaviour lives with that module, and `sidebar::timing` keeps only the sidebar's own cadences.

Site counts on the pass-4 rows below are taken at the pass base `8a4ca7bcd`, not at the program baseline.

Ordered teardown is room-owned, not deferred. `teardown_room` and `TeardownReport` move whole to `room/teardown.rs`, keeping the four steps in their original order: kill the session, purge the resurrection cache, `sidebar::sweep_orphan_runtime`, then `mux::recovery::sweep_orphan_processes` (`crates/rimz/src/room/teardown.rs:35-41`). The alternative the pass-3 planning left open, hoisting the runtime sweep out and leaving the function in `mux`, is rejected: the hoist runs the runtime sweep after the process sweep, and the process sweep kills leaked sidebar daemons (`mux/recovery.rs:131`), which moves the heartbeat and socket mtimes `sweep_orphan_runtime` decides on (`sidebar/mod.rs:387-405`). `mux/recovery.rs` keeps the guarded process sweep alone.

Superseded in part by the [pass 5 target](#pass-5-target): `wakeup` drops from beside `mux` above `store` to L2 below it. The rest of this direction stands.

### Pass 5 target

The direction the pass writes into `crates/rimz/src/mux/AGENTS.md` and `crates/rimz/src/store/AGENTS.md`: the wire is the leaf `wakeup` module below `store` at L2, and every sender (the store's write tail, `mux`, `remote_control`, the sidebar graph and its renderer, the CLI) reaches down to it, none reaching another module through it. The identifiers that cross the wire move to the modules that own identity: `MuxClientId` and `FocusNonce` to `ids` (`ids.rs:68`, `ids.rs:729`; the nonce stays a transparent `Uuid`, so the `FocusIntent` datagram and the `rimz.focus-anchor.v3` file are byte-identical), `ClientPaneView` to `pane` (`pane.rs:21`).

The rest of the pass sends vocabulary to the module that owns it. Shell selection (`user_shell`, `user_shell_program`, `shell_pane_name`, and the private probe helpers) is a process fact and lives in `proc` (`proc/mod.rs:85-101`); the room identity pin `ENV_CHANNEL`/`ENV_WORKTREE_PATH` and its argv rendering (`channel_shell_argv`, `channel_label_shell_argv`) join `ENV_WORKSPACE_ID`/`ENV_PROJECT_ROOT` in `workspace` (`workspace.rs:56-100`). `harness::launch` keeps agent-scope identity, argv compilation, the login-shell wrapper, and preflight. The persisted record's vocabulary is `agents`: `BudgetWindow`, `BudgetScope`, `BudgetPark` in `agents/state.rs:38-75`, `AgentState::channel()` in place of `harness::target::agent_channel` (`agents/state.rs:855`), and `LifecycleSignal::terminal_disposition` returning `TerminalDisposition` (`agents/lifecycle.rs:195-206`) in place of `harness::run::terminal_status_for_signal`. `harness` keeps the policy over that record: `BudgetSpec`/`BudgetParseError` parsing, ledgers and park constructors, `RunStatus` and the fold that maps a disposition onto it, and the verdicted `agent_handle`.

Pass 5 measures against `f43250479`. Admission rows retain their baseline counts and state changes in their reasons; the pass-log row records the measured deltas from `cargo xtask atlas diff --expect`.

### Pass 7 target

`store` owns every record it persists and reaches nothing in `harness`; `harness` is policy over store records and reaches down. `store::run` owns the run schema, codec, shared cancellation/timeout/budget terminal transition and wake sender; `store::event` owns the persisted signal vocabulary; `store::message` owns submitted-prompt alignment and the shared notice-header spelling. Handle minting and validation live in `agents::petname`. Harness retains run policy and workspace-lock wrappers, the run waiter, signal selectors and firing, and the message-header composer. Stored records, wire frames, grammar and observable behavior stay unchanged; only the impossible run-wake serialization error path is deleted.

### Pass 8 target

`store` owns every record it persists, the link-health tier included, and reaches up only into `diag`; disk mechanics, usage measurement included, sit below it in `disk`.

`LinkTier` lives beside `SidebarLinkHealth`, exposed as `store::snapshot::LinkTier`; remote keeps the classifier and thresholds over it. `disk::usage` owns the unchanged hardlink-aware byte walk and storage-root measurement. `FileIdentity` stays private: per-walk (dev, ino) dedupe, `parse_cache::FileStamp` mtime/length identity, and `temp_sweep` nlink exhaustion are three different rules, not a shared identity abstraction.

The extension puts OSC notification policy at L5 over config and mux capabilities, plugin-command failure vocabulary beside its diagnostic sample, and executable-path resolution solely in `proc`. `forge::pr_state` owns PR-state records and their reader; the sidebar retains forge probes, publication, and account-cache fusion. The generic default-on-unreadable cache read joins cache publication in `disk::atomic`. The two harness account-cache writers stay until their lift beside `agents::account`'s reader; the store/diag cycle closes from the store side, not by lowering diagnostics.

Superseded in part by the pass 16 target: `LinkTier` lives in `ids` and `diag` sits at L3 below `store`; the store/diag cycle closed from the diag side.

### Pass 9 target

Codex and Claude expose only registry adapters, delegated OAuth usage, and the Codex broker entrypoint; provider interiors and test helpers stay inside their adapter. One framed child transport and initialize handshake serve Codex enrichment and the warm broker, while broker caching, credential-stamp respawn, retry policy and request budgets remain local. One app-server observation owns account projection and refresh throttling. Both providers return neutral runtime-control outcomes directly. Claude derives parking from its typed Stop payload, uses the shared current-usage schema, and shares its priced-record gate; Codex keeps spend wire types beside their parser. Wire shapes, validation, persistence, and provider-specific discovery remain unchanged.

### Pass 16 target

`diag` is evidence vocabulary and append mechanics over `disk`, `ids`, and process facts; it sits at L3 below `store`, which constructs diagnostics downward and never reads one back. `LinkTier` lives at L1 in `ids` beside the other shared classification enums; store records, diagnostics, remote classification and sidebar notification policy import it directly. The store has no upward admission left. Inside diagnostics, one sink admission point owns disabled, limiter and poison handling, and the shared rotating reader owns generation traversal; the wire schema and per-surface logs remain local.

## Module verdicts

One row per module reviewed, at the granularity `survey` ranks. `holds` names the SHA reviewed and the scoped-commit count that reopens it (`git log --oneline <sha>.. -- <path> | wc -l`); `survey` reads these two cells and flags the module `held` until the count is reached, then `reopen`. A module pass that landed ends the same way, spelled `holds; landed pass-N` with the record commit's parent as the SHA (the last commit whose source was reviewed; [refactor-program.md](./refactor-program.md#ending-a-pass) fixes the rule), since only a `holds` status demotes the module in the rank; a bare `landed` marks a seam pass that reviewed the module's edges and left its interior a candidate. `candidate` is a survey pick not yet reviewed, with the signal that made it one.

| module | status | sha | reopen at | note |
| --- | --- | --- | --- | --- |
| `address` | holds; landed pass-14 | `0a9330ef1` | 30 | grammar and renderer stay with pane binding, launch-instance grouping and lineage at L4; one channel reconciler and one iterator launch selector; no binding-kind flag or root error re-export; eight message-only items narrowed. |
| `store` | landed pass-1; pass-7; pass-8; pass-15a; pass-16; pass-17b | — | — | seam reviewed; module interior remains a candidate. Pass 7: it owns the run record (`store::run`), the signal vocabulary (`store::event`), and the submitted-prompt alignment (`store::message`); it imports no `harness` item. Pass 8: owns `LinkTier` at `store::snapshot`; only diag upward admissions remain. Pass 15a: snapshot interior rethought; diag edges kept, re-layer queued as seam #10. Pass 16: seam reviewed, diag below; no upward admission remains. Pass 17b: writer interior rethought; reset runs through the boundary. |
| `store/snapshot` | holds; landed pass-15a | `922b15292` | 30 | snapshot-owned live projection and grouping; one private reducer owner; no std/sibling pass-throughs; six items narrowed to reach; wire, fold, binding and assemblers hold. |
| `store/writer` | holds; landed pass-17b | `793e3fd0a` | 30 | one log boundary with four cache policies, reset included; one publish tail; one receipt rule; one republish rule; launch vocabulary, queue method pairs, reap, lifecycle threshold seam and outcome types hold. |
| `agents` | holds; landed pass-1; pass-5; pass-7; pass-18a | `a3d0b8851` | 30 | seam reviewed pass 1/5/7 (budget vocabulary, `AgentState::channel()`, `TerminalDisposition`, `agents::petname`). Pass 18a: interior reviewed; the root exports only the names outside callers spell, hook representation is private to `hook_types`, test-only helpers are gated (`cfg(test)`, or `testkit` where the binary target's tests reach them), uncalled accessors are gone; the six never-overridden install defaults stay trait defaults by contract; schema types, `COMPACTING_WINDOW_SECS` and `DEFAULT_TOOL_REPEAT_ATTENTION_AFTER` hold. Adapter, spending and petname interiors keep their own rows. |
| `agents/adapters` | landed pass-2 | — | — | sibling seam reviewed; provider interiors remain separate module candidates |
| `daemon_view` | holds; landed pass-15c | `0175c3c6b` | 30 | pass-3 seam verdict superseded: loop-panel acquisition is one operation; dead repair chain and marker reach gone; spec, identity, repair machine and elder tracker hold; both cycles hold by intent. |
| `remote_control` | holds; landed pass-15c | `0175c3c6b` | 30 | one enable preflight; typed snapshot, batch toggle and advisories hold. |
| `room` | holds; landed pass-15c | `0175c3c6b` | 30 | constructors, birth, teardown, seven liveness policies and three flag parameters hold; LiveSessions moves to mux::selection, closing reload ↔ room and remote_control ↔ room. |
| `pane` | landed pass-3; pass-5 | — | — | seam reviewed; interior remains a candidate. Pass 5: it gains `ClientPaneView` (`pane.rs:21`) |
| `workspace` | landed pass-3; pass-5 | — | — | seam reviewed; interior remains a candidate. Pass 5: it gains the channel and worktree env keys and the channel shell argv (`workspace.rs:56-100`) |
| `mux` | landed pass-4; pass-5; pass-10 | — | — | seam reviewed; it now owns the focus anchor, the room width target, and the Zellij topology/presence-desired caches; interior remains a candidate. Pass 5: it imports no `harness` item. Pass 10: `mux/zellij` interior reviewed; `PresencePluginOptions::from_config` owns the config projection |
| `mux/zellij` | holds; landed pass-10 | `173682d90` | 30 | presence lifecycle (upgrade, converge, dump, fresh-topology wait) reviewed and deepened; backend, sidebar, session, layout and raw_pane interiors reviewed; topology schema stays the public wire; socket, pane_pid, parse and reap interiors hold |
| `sidebar` | landed pass-4 | — | — | seam reviewed; it keeps producer election, fusion, refresh lanes, and its own cadences; refresh interior landed pass-15b, other interiors remain candidates; produce interior landed pass-17a |
| `sidebar/refresh` | holds; landed pass-15b | `fbbe60d34` | 30 | one provider refresh entry, realtime merge and rate-limit transaction; PR computation shares ownership handling and emits name/payload pairs; consumer owns lane inputs; lane internals narrowed. |
| `sidebar/produce` | holds; landed pass-17a | `39b72afea` | 30 | named entries kept (no fold-mode core); CLI refresh pair and resolution mux parameter hold; panes/carry/starts/validate, metrics, tab_status, and git interiors reviewed and hold; metrics and git are one file each. |
| `wakeup` | landed pass-5 | — | — | new module in pass 4: the wire lifted out of `sidebar` (heartbeat record, event vocabulary, datagram send). Pass 5 re-layers it to L2, a leaf below `store` |
| `ids` | landed pass-5; pass-16 | — | — | seam reviewed: it owns shared identifiers, gaining `MuxClientId` and `FocusNonce`; interior remains a candidate. Pass 16: gains `LinkTier`. |
| `proc` | landed pass-5 | — | — | seam reviewed: it owns process and environment facts including shell selection; interior remains a candidate |
| `harness` | landed pass-5; pass-7; pass-8; pass-14 | — | — | seam reviewed: policy over the agent record (budget parsing, ledgers, `RunStatus`, `agent_handle`), with record vocabulary in `agents`; interior remains a candidate, and `harness/schedule` keeps its own row. Pass 7: policy over store records (run transitions and their workspace lock, the run waiter, signal selectors and firing); the run record and `RunStatus` live in `store::run`, `petname` in `agents`. Pass 8: reaches forge for PR state; two account-cache writer sites stay until the agents::account lift. Pass 14: address grammar, rendering, pane binding and launch lineage live in top-level `address`; launch-channel precedence stays in `harness::spec`. Pass-14b: schedule interior reopened with one arming rule, compiled delivery receipts and no instance forwarding hops |
| `agents/adapters/codex` | holds; landed pass-9 | `7af6d9f74` | 30 | app-server, broker, rollout, install, account and spend interiors reviewed; private transport/schema duplication removed; discovery, rollout presence semantics, pane-confirmation seam and conformance fixture hold |
| `agents/adapters/claude` | holds; landed pass-9 | `7af6d9f74` | 30 | remote-control, install, lifecycle, statusline, account and spend interiors reviewed; neutral control outcomes and shared priced-record gate; remote-control trio, typed Stop validation and discovery remain separate |
| `config` | holds; landed pass-12 | `9ea1ad057` | 30 | effective loader takes the machine snapshot; one glyph-set fold; registry constructors, defaults, validators and lookup hints private; resume ceiling owned by config; the fragment fold, editor, section records and presentation wire hold |
| `message` | holds; landed pass-13 | `efe38f50c` | 30 | interior reviewed: 31 items narrowed, synthetic queueing fixed to one System shape with auto-continue through `nudge_now`, settle owned by the `message deliver` helper, live-send pacing constructed inside `send`, `DispatchErr::Send` deleted; dispatch request/result wire, ordered check family, reply machine, compact and fire hold; upward edges reviewed (five keep, `harness::target` edge closed pass-14); reviewed SHA retained after rebase, and this pass's own commits count toward reopening |
| `harness/schedule` | holds; landed pass-6; pass-14 | `bee9e413d` | 30 | baseline esc 168, depth 22.2; pass 6: esc 147, depth 34.4, watcher and stop lifetimes hidden; pass 14: one arming rule, jiff weekdays, compiled armed receipt, exit codes from RunStatus; esc 180→176 |
| `agents/spending` | holds; landed pass-13 | `3c7dcbb18` | 30 | publication seam (one writer per cache, version predicates, shared readers), nested scoped result, one engine election path, one counted payload, spending-private mechanics; parser wire, walker, fold, discovery, service and cache records hold |
| `cli/remote` | candidate | — | — | pin, thin, cx 34.0, pace 1.32; pins are the prerequisite |
| `sidebar_pane/render/sections` | holds; landed pass-18b | `134e4e8db` | 30 | the full-frame suite (`render/tests`, 33 snapshots) already pins root → compose → chrome/sections, so no prerequisite pins; rename-only helpers removed and the scrollbar pads through `layout::pad_line_to`; `text_width`/`clip` are the layout vocabulary; width budgets are distinct rules (the one shared trim-then-pad rule is now single); `top_lines`/`scroll_lines`, theme lookups, `UiState`/`HitRegion` reach hold. Reviewed as the `render` bundle (root, compose, layout, sections). |
| `sidebar_pane/app` | holds; landed pass-11 | `f0d27f230` | 30 | loop transitions/context, event dispatch, folds, maintenance, focus repair, fetch/state and input interiors reviewed; width control and the three distinct elder-gated workers hold |
| `remote` | landed pass-8; pass-16 | — | — | keeps classifier and thresholds, imports the tier downward; module interior remains a candidate. Pass 16: tier imported downward from ids. |
| `disk` | landed pass-8 | — | — | gains `disk::usage`, the unchanged byte walk and storage-root measurement |
| `worktree` | landed pass-8 | — | — | reaches `disk::usage` downward; interior remains a candidate |
| `diag` | holds; landed pass-16 | `6fccc2b04` | 30 | Pass 8: imports the tier from `store::snapshot` and owns `PluginCommandFailure`; its sidebar upward admission is closed; store ↔ diag::record cycle retained for the store-side close. Pass 16 supersedes the old tier home and cycle disposition: evidence vocabulary and append mechanics at L3 below store; one admission point; shared rotated read; wire, per-surface logs, test-facing constructors and readers hold. |
| `forge` | landed pass-8 | — | — | owns the PR-state record and reader in `forge::pr_state` |
| `osc` | landed pass-8 | — | — | re-layered to L5 as policy over config and mux capabilities |
| `build_id` | landed pass-8 | — | — | no crate imports; proc alone owns executable-path resolution |

## Admission intents

One row per upward dependency edge reviewed. `keep` means the edge is the intended shape and its reason; `close` names the seam pass that closes it. An edge with no row is unreviewed, and `survey` lists it under `unreviewed` until a row lands; keep the edge cell machine-readable as `` `from` → `to` `` with an optional `to::{a,b}` group. Baseline: every admission in `refactor-target.toml` is unreviewed.

| from → to | sites at baseline | intent | reason / seam |
| --- | ---: | --- | --- |
| `store` → `agents` | 134 | keep | intended direction: the store folds the agent model (pass 1) |
| `agents` → `store` | 46 | closed | pass 1 |
| `store` → `message` | 22 | closed | pass 1 |
| `config` → `store::message` | 2 | keep | `AutoCompact` is config vocabulary and persisted record data; the persisted home wins |
| `agents` → `mux` | 6 | closed | pass 3 |
| `agents` → `daemon_view` | 3 | closed | pass 3 |
| `agents` → `diag::rotating` | 3 | closed | pass 3 |
| `pane` → `store::snapshot` | 1 | closed | pass 3 |
| `transcript` → `store::snapshot` | 1 | closed | pass 3 |
| `workspace` → `store::workspace_record` | 3 | closed | pass 3 |
| `agents` → `worktree` | 7 | closed | pass 3 |
| `store` → `daemon_view` | 2 | closed | pass 3 |
| `store` → `worktree` | 4 | closed | pass 3 |
| `mux` → `daemon_view` | 3 | closed | pass 3 |
| `workspace` → `worktree` | 1 | closed | pass 3 |
| `agents` → `child_process` | 2 | closed | re-layer: `child_process` is a process-lifecycle module and sits at L2 (pass 3) |
| `message` → `child_process` | 2 | closed | re-layer: `child_process` is a process-lifecycle module and sits at L2 (pass 3) |
| `diag` → `child_process` | 2 | closed | re-layer: `child_process` is a process-lifecycle module and sits at L2 (pass 3) |
| `daemon_view` → `remote_control` | 4 | closed | re-layer: `daemon_view` sits at L6, same layer (pass 3) |
| `agents` → `harness::target` | 2 | closed pass-14 | `agent_handle` is the canonical address; `agent_channel` becomes `AgentState::channel()` in a later pass. Pass 5 folded it in (`agents/state.rs:855`) and deleted `agent_channel`, leaving the one verdicted `agent_handle` site; re-keyed as `agents → address` in pass 14 |
| `agents` → `address` | 1 | keep | `agent_handle` is the canonical routable address; its renderer needs target resolution and launch occupancy, so attribution retains the one verdicted upward renderer call (pass 14) |
| `agents` → `harness::budget` | 1 | closed | later pass: `BudgetPark`/`BudgetScope`/`BudgetWindow` are `AgentState` record vocabulary. Closed pass 5: the trio is defined in `agents/state.rs:38-75` |
| `agents` → `harness::run` | 1 | closed | later pass: a `LifecycleSignal` terminal disposition in `agents::lifecycle`, mapped to `RunStatus` in harness. Closed pass 5: `TerminalDisposition` at `agents/lifecycle.rs:195` with the `RunStatus` map in the harness fold |
| `theme` → `agents` | 5 | keep | catalog and spec vocabulary are the shared language below the store |
| `trust` → `agents` | 1 | keep | catalog and spec vocabulary are the shared language below the store |
| `proc` → `agents` | 4 | keep | catalog and spec vocabulary are the shared language below the store |
| `config` → `agents` | 14 | keep | catalog and spec vocabulary are the shared language below the store |
| `mux` → `sidebar` | 27 | closed | pass 4 |
| `mux` → `remote` | 1 | closed | pass 4: `REMOTE_LINEAGE_ENV` is process vocabulary and moves to `proc` |
| `store` → `sidebar::{heartbeat,timing,wakeup}` | 4 | closed | pass 4 |
| `remote_control` → `sidebar::wakeup` | 1 | closed | pass 4 |
| `config` → `sidebar::timing` | 3 | closed | pass 4: the refresh trio is config vocabulary and is defined in `config` |
| `store` → `wakeup` | 2 | closed | seam 6: the wire moves below the store once `ClientPaneView` reaches `pane` and `FocusNonce` reaches `ids`. Closed pass 5: both types moved, `wakeup` re-layered to L2, and the two sites (`store/gc/collect.rs:12`, `store/writer/publish.rs:180`) keep their code as downward calls |
| `store` → `mux` | 1 | closed | pass 5: the site was the client-view type the wire carried; the snapshot reads `crate::pane::ClientPaneView` now (`store/snapshot/view.rs:100`) |
| `daemon_view` → `sidebar::timing` | 1 | keep | `EVENT_PANE_TTL` is the sidebar's event-mode cadence; daemon maintenance accepts a frame under it and reaches up for the bound deliberately |
| `daemon_view` → `daemon_content` | — | keep | pass 15c: resolve_content keeps layout slot cardinality and the supervisor's default/invalid-pane policy one decision |
| `daemon_view` → `sidebar::frame` | — | keep | pass 15c: the elder reads the published PaneFrame as disposable topology truth and falls back to authoritative repair |
| `daemon_view` → `sidebar::cache` | — | keep | pass 15c: freshness verdict and zero-child cache read; a stale frame never suppresses repair |
| `room` → `sidebar` | — | keep | pass 15c: birth purges rebirth heartbeats only after proving the session absent, supplies options while sidebar keeps election, and teardown orders the orphan sweep after session death and before the process sweep |
| `room` → `sidebar::body_filter` | — | keep | pass 15c: pristine birth resets presentation state (0e3b80c55) |
| `room` → `reload` | — | keep | pass 15c: ownership stages through reload's one durable-build path (f7e1b4153, 28c3552ba) |
| `mux` → `harness::launch` | 5 | closed | later pass: shell-program and pane-name vocabulary belongs below `mux`. Closed pass 5: shell selection to `proc`, the room pin's channel/worktree keys and channel argv to `workspace` |
| `config` → `harness::budget` | 5 | keep | `BudgetSpec` and `BudgetParseError` are parsing, which is harness policy; the window/scope/park lift takes the edge from 5 sites to 4 in pass 5 |
| `store` → `harness::schedule::signal` | 5 | closed | added by `a8e4c4afa` after pass 4 (`store/event.rs:9`, `store/follow.rs:10`, `store/writer/signal.rs:3`); pass 6 defers a split moving `SignalName`/`SignalNameErr`/`SignalSource` below store and making `append_signal` take `SignalEventPayload`. Closed pass 7: the three types are defined in `store::event` beside `SignalEventPayload`, and `append_signal` takes the payload that `harness::schedule::signal` projects through `impl From<&Signal>` |
| `config` → `harness::schedule` | — | keep | pass 6: trigger and surplus grammar are harness policy, matching the pass-5 budget-parsing precedent |
| `store` → `harness::petname` | 4 | closed | pass 7: `store` owns every record it persists and reaches nothing in `harness`; the handle grammar moves whole to `agents::petname`, whose floor is already L2 through `agents::known_kinds()` |
| `store` → `harness::target` | 1 | closed | pass 7: same direction; `align_submitted_prompt` aligns against `store::message` records and lives there with `HarnessNotice::header_type`, and the harness header composer calls the method |
| `store` → `harness::run` | 6 | closed | pass 7: same direction; the run record's schema and codec are `store::run`, and the transitions, their workspace lock and the lock-then-codec wrappers stay harness policy |
| `store` → `harness::run_wake` | 1 | closed | pass 7: same direction; the terminal wake sender, `WakeupFrame` and `run_socket_path` join the record in `store::run`, and the waiter stays in `harness::run_wake`, importing them downward |
| `store` → `diag` | — | closed pass-16 | pass 15a: the store constructs the five bind diagnostics it can explain; a store-side close (snapshot-owned enum + From in diag) costs +65 SLOC and duplicates the vocabulary cli/doctor and sidebar/frame freeze; the cycle closes by re-layering diag to L3 with LinkTier below store — seam #10, contradicting the pass-8 target, queued for review. Pass 16: re-layer, diag at L3 and LinkTier in ids; the four store-to-diag sites stay as downward calls. No diag-to-store intent row is needed at zero sites. |
| `store` → `diag::record` | — | closed pass-16 | pass 15a: the store constructs the five bind diagnostics it can explain; a store-side close (snapshot-owned enum + From in diag) costs +65 SLOC and duplicates the vocabulary cli/doctor and sidebar/frame freeze; the cycle closes by re-layering diag to L3 with LinkTier below store — seam #10, contradicting the pass-8 target, queued for review. Pass 16: re-layer, diag at L3 and LinkTier in ids; the four store-to-diag sites stay as downward calls. No diag-to-store intent row is needed at zero sites. |
| `store` → `remote::link` | — | closed | `LinkTier` is a value type; `remote::link` depends on `mux::CommandSpec`, so the type moves down, not the module. Closed pass 8: tier owned by `store::snapshot` |
| `store` → `disk_usage` | — | closed | `dir_size` is a disk fact; `disk_usage.rs` depends only on `disk::paths` and belongs beside `disk`. Closed pass 8: whole module moved to `disk::usage` |
| `worktree` → `disk_usage` | 1 | closed | pass 8: re-layer with the move to `disk::usage` |
| `harness` → `sidebar::refresh::pr` | 4 | closed | pass 8: the PR-state record and reader are `forge::pr_state` |
| `harness` → `sidebar::refresh` | 2 | keep | account-cache writers (`merge_account_rate_limits`, `merge_provider_realtime_usage`) embed refresh-lane fusion; reaffirmed in pass-15b without changing either harness caller; close: lift the writer beside `agents::account`'s reader in a later pass |
| `osc` → `config` | 3 | closed | pass 8: re-layer, OSC is policy over config vocabulary and mux capabilities and sits at L5 |
| `osc` → `mux` | 2 | closed | pass 8: same re-layer to L5, backend facts remain in mux |
| `diag` → `sidebar::presence` | 1 | closed | pass 8: `PluginCommandFailure` is owned by `diag::plugin_presence`, which persists it |
| `build_id` → `proc` | 1 | closed | pass 8: the forwarder had no caller; proc owns the resolver |
| `config` → `harness::spec` | 9 (pass-12 base) | keep | pass 12: the loader holds the ordered fragment set and source attribution; harness owns layout validation and resolution. Moving validation to callers loses strict/lenient parity and read-only consumers |
| `config` → `harness::resume` | 1 (pass-12 base; also counted by the resume file rule) | closed | pass 12: the resume ceiling is a config default, defined privately in `config/resume.rs`; both directory and file admissions close |
| `message` → `harness::target` | 17 (+3 counted again by the `send.rs` file rule) | closed pass-14 | address/pane grammar (`resolve_many`/`resolve_targets`/`resolve_agents`, `pane_binding`/`bind_agent`, `agent_handle`, `addressable_agents`, `is_broadcast`, `group_prefixed`, `shadowed_by_pane_owner`, `message_header`, `recipient_channel`) belongs at L5 or lower with its parser/renderer kept together; `harness/target.rs:890-1115` mixes it with launch/cohort functions and reaches up to `worktree::dashed_name` (`:134-156`), so the split is a seam pass, not a module edit; `message/AGENTS.md:28` moves with it; closed by the L4 `address` home, with branch-name mapping in `ids` and launch-channel policy in `harness::spec` |
| `message` → `harness::ancestry` | 2 | keep | sender exclusion from `@all` needs durable launch identity after resolution (`dispatch.rs:399-434`); loop dispatch supplies no caller |
| `message` → `sidebar::produce` | 4 | keep | dispatch chooses a rollup-only or fresh frame+rollup fold after inspecting pending records (`dispatch.rs:249-291`); `deliver_one` and the sweep need the same fold |
| `message` → `harness::auto_continue` | 1 | keep | `ResumeUnrecovered` re-verifies a `Resume` park at delivery time; recovery knowledge is auto-continue's (`auto_continue.rs:144-171`) |
| `message` → `harness::run` | 1 | keep | `report::digest_fully_joined` is the before/after-claim digest guard (`deliver.rs:240-289,322-341`; messaging.md § The subagent-digest join guard) |
| `message` → `harness::assist_log` | 3 | keep | the auto-compact assist is observable only where the synthesized command reaches `Sent` (`send.rs:100-111`) |

## Pass log

One row per pass, newest last. Deltas are the `diff --expect` totals at merge.

| date | pass | scope (`paths`) | base | verbs landed | Δ prod SLOC | Δ esc | Δ dep sites | deferred |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 2026-09-03 | pass-1 seam `store` ⇄ `agents`, `store` ⇄ `message` + `disk` lift | `crates/rimz`; `xtask/src/invariants.rs`; `docs`; `refactor-target.toml`; `AGENTS.md`; `CLAUDE.md`; `ARCHITECTURE.md` | `3e01afc71` | rehome ×7, delete ×7 | −4 | +4 | +20 | Seam 4: `workspace_record`/`compose_channel` homes; seam 5: agents → peer modules; `for_terminal_status` kept. Tightened surfaces: `store` 321, `message` 90. Contract ceiling loosened −20→−1 because the estimate omitted the one-line-per-file import splits a no-shim lift imposes and the method signatures the record's merge rules gained; measured −2 at step 7, −4 after the review round. Atlas advisories: multiple destination definition sites are reported as “not defined under”; cfg-gated items count as production while files behind an equivalent cfg-gated module do not. |
| 2026-09-04 | pass-2 seam adapter sibling families | `crates/rimz/src/agents`; `docs`; `refactor-target.toml` | `93c4c48d7` | rehome ×1, deepen ×1, collapse ×1 | −45 | −2 | +1 internal (crossing +0) | Five shape families and the cached-value test guard hold for the reasons recorded above. No upward edge was reviewed. Deferred module notes: Copilot/Cursor `report_files`, Droid origin stamping, and the filename-scoped `spend.rs` invariant. Tightened `agents` surface to 970. |
| 2026-09-04 | pass-3 seam #4 + #5 + `daemon_view` ↔ `mux` | `crates/rimz`; `docs`; `refactor-target.toml`; `AGENTS.md`; `ARCHITECTURE.md` | `d8361c248` | rehome ×10, deepen ×1, re-layer ×2 | +5 | −6 | +4 internal | `AgentState::channel()`; `BudgetPark`/`BudgetScope`/`BudgetWindow` into `agents`; `LifecycleSignal` terminal-disposition split; `PaneRef` method forms; `CommandSpec` runner collapse; Codex daemon spawn-failure and timeout warning text changes as declared. |
| 2026-09-05 | pass-4 seam #3 remaining `mux` → `sidebar` | `crates/rimz`; `xtask/src/invariants.rs`; `docs`; `refactor-target.toml`; `AGENTS.md`; `CLAUDE.md`; `ARCHITECTURE.md` | `8a4ca7bcd` | rehome (31 contract rows), deepen/collapse focus dispatch, delete duplicate clocks and unused nonce/default/error surface | −44 | +2 | −4 internal | Measured with `cargo xtask atlas diff --expect /tmp/atlas-pass-4-contract.toml`: test SLOC +62, Rust files +2; all six dependency rows at 0. Ordered teardown moved whole to `room::teardown`, not a reordered sweep hoist. Seam 6 defers wire-below-store; mux's five launch-vocabulary sites remain for a later pass. Kept separate heartbeat freshness rules and sidebar-owned `EventStore`; dropped `execute_action`'s unused nonce return; `FocusPresentation` stays public for benchmark-facing fusion. Tightened sidebar 367, mux 277, wakeup 26. |
| 2026-09-05 | pass-5 seam #6 + deferred launch/budget/lifecycle admissions | `crates/rimz/src/wakeup`; `crates/rimz/src/mux`; `crates/rimz/src/pane`; `crates/rimz/src/ids.rs`; `crates/rimz/src/proc`; `crates/rimz/src/workspace`; `crates/rimz/src/agents`; `crates/rimz/src/config`; `crates/rimz/src/store`; `crates/rimz/src/harness`; `crates/rimz/src/message`; `crates/rimz/src/room`; `crates/rimz/src/web`; `crates/rimz/src/sidebar`; `crates/rimz/src/sidebar_pane`; `crates/rimz/src/diag`; `crates/rimz/src/cli`; `crates/rimz/tests`; `docs`; `refactor-target.toml`; `AGENTS.md`; `CLAUDE.md`; `ARCHITECTURE.md` | `f43250479` | rehome ×13, delete ×2, deepen ×1, re-layer ×1 | −13 | +5 | −9 internal (crossing +0) | Measured with `cargo xtask atlas diff --expect /tmp/atlas-pass-5-contract.toml`: test SLOC +286, Rust files +0; all 13 rehomes, two deletes and nine dependency rows landed. Ceiling +16 paid for the terminal-disposition enum and split budget imports; shorter channel calls and import formatting offset that cost. Escaping surface grows +5 with the lifecycle classification and agents budget façade; five upward admissions close, and agents reaches harness only for `agent_handle`. Deferred: the room-pin map/argv duplication (`room/mod.rs:88-99` vs `workspace::channel_shell_argv`) and transparent `FocusNonce`. Tightened harness 603, mux 273, harness/launch 41, harness/run 41. |
| 2026-09-05 | pass-6 module `harness/schedule` | `crates/rimz/src/harness/schedule.rs`; `crates/rimz/src/harness/schedule`; `crates/rimz/src/cli/loop_cmd`; `crates/rimz/src/cli/wake`; `crates/rimz/src/cli/events.rs`; `crates/rimz/src/cli/hooks/lifecycle/observe.rs`; `crates/rimz/tests/integration/{loop_schedule,wake}.rs`; harness contract; loop internals; ledger; `refactor-target.toml` | `bcd1542a8` | delete unused metadata/accessors/planner/path seams; deepen stop/watcher lifetimes and schedule projection; rehome held labels, delayed time and task keys | −136 | −51 (198→147) | −10 internal; +2 crossing (+3 downward, −1 unranked) | Measured with `cargo xtask atlas diff --expect /tmp/pass-6.toml`: test SLOC +18, Rust files +0, watcher assembly 13→1; all eight deletes landed. Esc ceiling 140→149 translates the original ≥49 reduction to the gate counter: inspect excludes module declarations and deduplicates reexports (189→138), while diff counts them (198→147). Source ceiling −90 unchanged. Deferred: store→signal vocabulary seam above; `TaskFire::complete` closures and an observation module rejected as sideways. Store setup remains eager for every held lock through the stop callback; lookup errors propagate after setup. Kept inferred binary-facing result types and the ten-input fire constructor. Tightened harness 559 and runner 35; the global tighten also pruned pre-existing launch admissions and lowered launch to 40. |
| 2026-09-06 | pass-7 seam #7 `store` → `harness` | `crates/rimz`; `docs`; `refactor-target.toml`; `AGENTS.md`; `CLAUDE.md`; `ARCHITECTURE.md`; `xtask/src/invariants.rs`; `xtask/src/invariants/tests.rs` | `97a947974` | rehome ×15 (+ the terminal transition onto `RunRecord`), delete ×1, deepen ×1 | −20 | +2 | +8 internal | Measured with `cargo xtask atlas diff --expect /tmp/pass-7-contract.toml`: test SLOC +178, Rust files +0; all 15 rehomes, the delete and six zero-site dependency rows landed. Store → harness 17→0; source ceiling 0 retained (D1 spike +15, final −20). Escaping surface +2 pays for four shared items minus two deleted wrappers; internal sites +8 include the split imports. Carried `d904fa4bd` first. Read-only invariant needles follow the moved wake sender, with regression coverage. Deferred: remote link vocabulary and disk usage as later seams, and diag re-layering if the survey ranks it. Kept `harness::run::{create, load, list}` wrappers and public `WakeupFrame` for binary callers. Tightened harness 536, run 31, run_wake 9; store 336 and agents 988. |
| 2026-09-06 | pass-8 seam #8 store edges plus small upward-edge extension | `crates/rimz/src/{store,remote,diag,sidebar,sidebar_pane,disk,cli,forge,harness,proc}`; `diag.rs`; `disk_usage.rs`; `worktree.rs`; `lib.rs`; `forge.rs`; `osc.rs`; `build_id.rs`; `crates/rimz/tests/integration/wake.rs`; `docs`; `refactor-target.toml`; `AGENTS.md`; `CLAUDE.md` | `6d13498b3` | rehome ×11, delete ×2 (manual order policy, unused resolver), re-layer ×2 (disk usage, osc → L5) | −17 | +4 summed (+3 unique) | +7 internal | Measured with `cargo xtask atlas diff --expect /tmp/pass-8-contract.toml`: test SLOC +61, Rust files +1; eleven rehomes, one contract delete and seven dependency rows landed. Six zero-site rows close nine sites; harness → sidebar::refresh falls 6→2, retaining account-cache writers. Esc totals overlap forge.rs/forge: the new module declaration counts twice. The extension is production-neutral: record extraction/imports offset deletion; the combined pass retains ceiling 0 and removes 17 SLOC. Store ↔ diag::record is now a cycle; close from the store side in the store/snapshot module pass, not by lowering diag. Deferred: account-cache writers beside agents::account, distinct FileIdentity/FileStamp/temp_sweep rules, remote interior. Four pins green before moves; disk body unchanged (header corrected later). Tightened remote 177, lib 69, build_id 6, sidebar 361; store 337, diag 80, disk 123/atomic 19, forge 48. Global tighten also pruned the already-unused runner → proc allowance. |
| 2026-09-08 | pass-9 module Codex + Claude adapters | `crates/rimz/src/agents/adapters/{codex,claude}`; agents contract; broker integration/fixture; `docs`; `refactor-target.toml` | `e30457536` | collapse child transport, control outcomes, usage schemas and priced-record gate; deepen observations/probes; delete relays/labels/forwarders; rehome spend wire | −240 | −147 (Codex 96→8, Claude 64→5) | −8 internal; +4 crossing (−1 same, +5 unranked) | Measured with `cargo xtask atlas diff --expect /tmp/pass-9-contract.toml`: test SLOC +68, Rust files −1; all six deletes and both surface ceilings landed, production ceiling −200 unchanged. Pins first, green on c5b3b8424; review added exact initialized-frame assertions for both paths and re-keyed verdicts to resolvable production items (test-only decisions stay here); proof pins the rebased merge-base SHA because local main diverged and atlas changed-path checking uses the mutable ref rather than its resolved merge base (`xtask/src/atlas/diff.rs:420`). Native OAuth errors retain crate visibility for inferred neutral-error sources; enclosing modules limit reach. Parking is derived, typed Stop fields retained; broker keeps both initialized wire shapes and resets handshake budget after the auth stamp. Tightened Codex 8 and Claude 5; global tighten also lowered the pre-existing mux ceiling 276→274. Deferred: rollout alias/presence table (not learned to per-file steps), headless serde flatten (unqualified), install toml macro construction (availability unverified); transcript lookup ignoring CODEX_HOME (`codex/transcript.rs:334`), substring daemon classification (`codex/process.rs:87`), per-attempt rather than total refresh budget (`codex/app_server.rs`), and initialized params divergence (reported, not fixed). Discovery, remote-control trio, conformance fixtures, test injection and provider OAuth headers hold; no upward admission was reopened. |
| 2026-09-08 | pass-11 module `sidebar_pane/app` | `crates/rimz/src/sidebar_pane/app.rs`; `app/**`; `supervise.rs`; `pets/painter.rs` gallery-only accessor; sidebar contract; `docs`; `refactor-target.toml` | `594690c7e` | deepen ×2, collapse ×3, delete ×8, rehome ×1; narrow and gate preview reach | −297 | app −5 (11→6); painter −1 (9→8) | +29 internal; −3 crossing (−4 downward, −3 same, +4 unranked) | Measured with `cargo xtask atlas diff --expect /tmp/pass-11-contract.toml`: test SLOC +20, Rust files +0; all three deletes, surface ceiling 8 and production ceiling −170 landed. Actual code net −147; feature gating moves 146 existing SLOC (demo 131, painter constructors/accessor 13, module/export 2) plus four new attributes into test support. Explicit imports pay for clearer ownership; no upward admission was reviewed. Tightened sidebar_pane 180→173 and app 11→6. Kept public SidebarAppErr for binary-facing testkit errors; authorized the one-line pets accessor gate to keep the default build warning-free. Pins green on 79dc85a60 before structural changes; rebased onto current trunk, not the still-unmerged pass 10. Width control and the three worker lifecycles hold. Deferred: FetchDiagnostics/emit_diagnostics inlining (tests construct the carrier), provenance-aware compute_next_state (732161ced/25a16ed5c), row_index_of_pane baseline-membership predicate, and fixtures.rs `_focused`; the worker drain was removed after proving single-flight dispatch. |
| 2026-09-08 | pass-10 module `mux/zellij` | `crates/rimz/src/mux/zellij`; `mux/zellij.rs`; `mux/mod.rs`; `mux/domain.rs`; `reload.rs`; `room/mod.rs`; integration `backend/zellij/{presence,self_close}.rs`; mux contract; multiplexer internals; ledger; `refactor-target.toml` | `161827c25` | deepen ×3 (upgrade, config projection, version gate); rehome fresh-topology wait; collapse reload poll shell; delete constructor, cache-root alias, converge flag, cleanup/hash façade, five constant parameters and stale strangler | −64 | −15 summed (Zellij 61→50; overlapping root 32→27; mux root +1) | −1 internal; −1 crossing (−3 downward, +2 unranked visibility paths) | Measured with `cargo xtask atlas diff --expect /tmp/pass-10/contract.toml`: test SLOC +98, Rust files +0, reload assembly 6→3; production ceiling −60 unchanged. Removed constructor proved by diff/grep: Atlas resolves its deleted `new` key to the remaining layout constructor and rejects an impl-qualified key. Pulled constructor deletion forward to keep the mux ratchet; privatized session-local workspace resolution and adapter-local version parsing to retain the ceilings. Kept public schema/backend/testkit construction and the explicit convergence verb for the integration crate; nested module ceiling 50. Tightened parent mux 274→260 and removed the unused session→config allowance; restored TOML comments discarded by the serializer. Rejected shared `poll_until` (six of fifteen schedules match, ≤14 lines before adapters; other loops pin 0d39d58f5, 4143aacb5, 3b23ae6c9, 29b7cd775, b0a1cb160), testkit topology-fixture writer (adds surface), artifact/log ownership in the backend (caller policy), companion sibling collapse (native differences), and doctor/sidebar freshness-selection collapse (distinct decisions). Advisory: topology dump no longer loads unused machine config or resolves an unused recorded binary, so their degraded-case diagnostics no longer originate there. The moved inspection-fallback debug event now uses the presence module tracing target (text, level and operation fields unchanged). Pins were green before structure on 79dc85a60; pass 11 merged first, and trunk later replaced its theme commit. Dropped that stale theme commit, then rebased onto the completed trunk glyph fix at 161827c25; the branch carries no theme or snapshot changes. Re-measurement retained every pass-10 delta and mux ceiling 260. No bugs fixed or other bugs found. |
| 2026-09-08 | pass-12 module `config` | `crates/rimz/src/config`; `crates/rimz/src/cli/agents_cmd`; `crates/rimz/src/cli/complete.rs`; `crates/rimz/src/cli/first_run.rs`; `crates/rimz/src/cli/setup.rs`; `crates/rimz/src/cli/hooks/lifecycle/observe.rs`; `crates/rimz/src/cli/subagents/mod.rs`; `crates/rimz/src/cli/supervised/run.rs`; `crates/rimz/src/cli/teams/list.rs`; `crates/rimz/src/harness/plan`; `crates/rimz/src/harness/rebirth.rs`; `crates/rimz/src/harness/resume`; `crates/rimz/src/sidebar/notify.rs`; `crates/rimz/src/sidebar_pane/app/tests.rs`; `crates/rimz/src/sidebar_pane/render/animation.rs`; `crates/rimz/src/theme/glyphs.rs`; `crates/rimz/src/theme/palette.rs`; `crates/rimz/tests/integration/config.rs`; `crates/rimz/tests/integration/message.rs`; `crates/rimz/tests/integration/resume.rs`; `crates/rimz/tests/integration/backend/zellij/resume.rs`; `crates/rimz/tests/integration/backend/tmux/agent_lifecycle.rs`; `docs`; `refactor-target.toml` | `9ea1ad057` | deepen ×1, rehome ×2, delete ×7, narrow ×19; replace setup's hand-kept path list with the registry | −104 | −20 (config 187→167) | −4 internal; −9 crossing (downward) | Measured with `cargo xtask atlas diff --expect /tmp/pass-12/contract.toml`: 24 contract paths, production ceiling −80 and config esc ceiling 170. Test SLOC +56; Rust files +0. Tightened config 187→167, harness 574→573, notifications 23→22, resume config 5→4, sidebar config 5→4 and harness resume 59→58; worktree config stays 4 and all four per-file rules remain. Removed the now-unused harness/plan → disk::paths admission. Restored all 18 TOML comments after tightening. Pins landed green before structure; deletes precede loader deepening so its additional explicit-root entry stays under the per-commit ratchet. The glyph-fold commit drops only redundant as_deref adapters from the base pins; expected values stay fixed.  config → harness::resume 1→0 (required 0). Declared setup fix: fresh writes report `Wrote …/loop.toml`, and a lone loop file now gets the interactive keep-config prompt instead of an unprompted overwrite. The new PTY test pins preservation of a task value (merging retains explicit values, not arbitrary old comments). No other output, error text, schema, template, default or ordering changes. Deferred: `ResumeConfig::auto_continue_backoff(retries)` absorbing the ramp interpretation in `harness/auto_continue.rs:439-476` (behaviour kept; `ce0c00897` pins the empty-ramp 300 s fallback); the `parse_text` seam taking a filename plus unused `agents_home` at `harness/schedule/config_edit.rs:42-46` (seam redesign outside this narrowing pass); typed `auto_redeem_min_gain` (storage/grammar change outside scope; `e6a4e8829` pins the fallback). Kept `parse_core_text_collecting` as a lifetime adapter for loader callbacks, not a pass-through, and all four per-file config rules for their local allow-lists. Atlas caveat 13 records named-reference narrowability's missing public field/signature constraints; public wire types hold. Advisory, not fixed: setup and room first-run checks still use the core config path alone. |
| 2026-09-09 | pass-13a module `message` | `crates/rimz/src/message.rs`; `crates/rimz/src/message`; `crates/rimz/src/cli/message`; `crates/rimz/src/cli/answer.rs`; `crates/rimz/src/cli/budget.rs`; `crates/rimz/src/cli/agents_cmd/auto_continue.rs`; `crates/rimz/src/cli/agents_cmd/budget.rs`; `crates/rimz/src/cli/agents_cmd/subagent_report.rs`; `crates/rimz/src/store/message/tests.rs`; `crates/rimz/tests/integration/hooks.rs`; `crates/rimz/tests/integration/message.rs`; `docs`; `refactor-target.toml` | `b1f86c1df` | narrow ×31, collapse ×3, rehome ×1, deepen ×1, delete ×1 | −33 (7232→7199) | −30 (message 97→67) | −3 internal (58→55); −5 downward (238→233) | Measured with `cargo xtask atlas diff --expect /tmp/pass-13a/contract.toml`: test SLOC +100 (12166→12266), Rust files +0; production ceiling −26 and message esc ceiling 68 met, surface tightened to 67. Spike production delta was −29; final downward reduction includes three qualified `MessageSender::System` references and two now-unused `DeliveryGate` imports. Full gate: 6490 passed. Module reviewed and pins green on `efe38f50c`; rebased measurements use `b1f86c1df`, whose upstream changes do not touch message. Pins grew 106 raw lines: auto-continue affinity is asserted at the `message.queued` enqueue event, then cleared on Retry; the deferred record is queued and unpinned. Settle pin checks only the elapsed lower bound. Auto-continue's fresh-message error contexts merge from “queueing auto-continue resume message” / “delivering auto-continue resume message” to “queueing and delivering auto-continue resume message” on nulled stderr; no reader exists. Corrected the settle documentation: it precedes the re-check and claim, not claim and acknowledgement. Twelve dossier “narrowable” rows are refuted by caveat 13; `DispatchResult` and `ReplyJoinResult`, reached only by inference through public signatures/fields, are the new instances. Deferred: `ReplyWait::run` (three methods → one plus `ReplyEvent`, line-neutral, timing pinned by `27077a848`/`cfe1240a3`); `compact_idle` absorbing idle preflight and assist accounting (line-neutral, needs `send_compact` to return the id and `CompactRequest` to lose `message_id`/`sender`/`automated`); the `harness::target` seam described in the intent row; moving `settle_duration_from_env` into cli (sideways). |
| 2026-09-09 | pass-13b module `agents/spending` | `crates/rimz/src/agents/spending`; `crates/rimz/src/agents/adapters`; `crates/rimz/src/agents/mod.rs`; `crates/rimz/src/agents/attribution.rs`; `crates/rimz/src/agents/pricing`; `crates/rimz/src/agents/AGENTS.md`; `crates/rimz/src/disk/paths.rs`; `crates/rimz/src/harness/budget.rs`; `crates/rimz/src/harness/budget`; `crates/rimz/src/sidebar/consumer.rs`; `crates/rimz/src/sidebar/enrich`; `crates/rimz/src/sidebar/refresh`; `crates/rimz/src/sidebar/timing.rs`; `crates/rimz/src/sidebar_pane/app/fetch/tests.rs`; `crates/rimz/src/cli/stats`; `crates/rimz/src/testkit`; `crates/rimz/benches/hotpath.rs`; `crates/rimz/tests/integration/common`; `crates/rimz/tests/integration/journey`; `crates/rimz/tests/integration/performance`; `docs`; `refactor-target.toml` | `b1f86c1df` | collapse ×4 (writers, engine arms, counted payload, provider reader), deepen ×1 (scoped result), rehome ×2 (version rule, filename rule), delete ×2, narrow ×30 | −205 | spending −31 (114→83); sidebar/refresh −1 (80→79); disk/paths +2 (65→67) | −3 internal; −1 crossing (−2 downward, +1 unranked) | Measured with `cargo xtask atlas diff --expect /tmp/pass-13b-contract.toml`: production SLOC 53063→52858, test SLOC 55037→55169 (+132), Rust files +0; both deletes, production ceiling −180 and spending esc ceiling 85 landed. Pins first, green on efe38f50c before structure; rebased onto b1f86c1df with identical content. Tightened agents 1005→965; new agents/spending rule 83; retained all other budgets and comments. Disk 124→126 pays for the shared filename predicate and unsorted runtime scan beside the constructor. The sequential prune spelling rejects the never-produced workspace-spending.json, while the constructor always emits a dot-separated hash; empty hashes remain accepted. No upward admission was reviewed. Re-keyed Default's verdict to the module/function key and the private sole-survivor policy to its consumer boundary so Atlas resolves both. Deferred: impl Default for SpendingWalker (clippy new_without_default and invariant needle); budget's local-day cutoff (harness/budget.rs:1247-1290 at base, different negative-time handling); SlotEffort merge and fresh-memo wrappers, add_spending_sessions/add_scoped_sessions traversal pair (sideways); sole-survivor and newest-day selectors stay consumer policy. Parser wire, walker, fold, discovery, service and cache records hold. |
| 2026-09-09 | pass-14a seam #9 `message` → `harness::target` | `crates/rimz/src/{address.rs,address,harness,message,message.rs,agents/attribution.rs,cli,lib.rs,ids.rs,worktree.rs,store/snapshot,store/AGENTS.md,sidebar/consumer}`; `crates/rimz/tests/integration`; `docs`; `AGENTS.md`; `CLAUDE.md`; `ARCHITECTURE.md`; `refactor-target.toml` | `463fc1426` | rehome ×32, collapse ×4, delete vestigial basename and binding kind, narrow ×8 | −47 | harness −34 (577→543); address +32; ids +1 (44→45); worktree −1 (58→57); lib unchanged 69 | −1 internal; 0 crossing | Measured with `cargo xtask atlas diff --expect /tmp/refactor-pass-14-atlas/contracts/pass-14a.toml`: production SLOC 87532→87485, test SLOC 126199→126221 (+22), Rust files +0; both deletes, all 32 rehomes and all seven dependency ceilings landed. Message→target 17→0, message→harness 24→7, agents→harness 1→0, agents→address 0→1, cli→target 74→0; address reaches neither harness nor worktree. Tightened address 32, harness 543 and worktree 57; other owned budgets unchanged. Pin first, green on the original implementation; final gate 6520 passed and contract exit 0. Raw Rust delta −33 separates into production −56 and tests +23 after splitting inline test hunks. Kept the Candidate channel-membership facade, durable pane-pin guard, launch-channel team option and durable CLI address fallback by verdict. Deferred: message ResolutionView absorption and CLI message_target fold; phase-2 schedule interior remains separate. |
| 2026-09-09 | pass-14b module `harness/schedule` | `crates/rimz/src/harness/{schedule.rs,schedule,AGENTS.md,CLAUDE.md}`; `crates/rimz/src/cli/{loop_cmd,wake}`; `crates/rimz/tests/integration`; `docs`; `refactor-target.toml` | `69f091c98` | collapse ×1, rehome ×3, deepen ×2, delete ×1, narrow ×6 | −25 | schedule 180→176; schedule.rs 46→45; harness 543→539 | +2 internal; 0 crossing | Measured with `cargo xtask atlas diff --expect /tmp/refactor-pass-14-atlas/contracts/pass-14b.toml`: production SLOC 10686→10661, test SLOC 68968→69021 (+53), Rust files +0; both esc ceilings and production ceiling −25 met, complete evidence. Raw production −30 / tests +57 after inline-test split. Pins first, green on the original scheduler; final target selection 395 passed, full gate 6522 passed. Initial −35 production ceiling and 175 esc estimate corrected to −25 and 176: the new enable boundary costs more than estimated, receipt/exit-map changes are line-neutral, and instance reset calls must retain distinct error prefixes; a named replacement key removes three more lines. Only harness budget lowered (539), runner stays 39. Kept public RunLockInfo, SignalSelector and LoopRunRecord::new; clock normalization, two-mode optional inputs, distinct team-instance spellings, CLI validation order, catalog-private resets, instance error paths, per-name retirement errors and CLI delivery orchestration remain. Candidate 6 rejected as sideways: moving the host predicate changes the Manual check-only branch pinned by runner/tests.rs:474-560 for no surface reduction. Advisories at the phase base: doctor.rs:146-160 mislabels check-only tasks; loop_cmd/add.rs:169 vs :357-368 preserves surplus_after trim asymmetry; runner.rs:926-928 has an unreachable Background arm; the old re-arm fence is absent despite instance-row CAS (instances.rs:147-154, catalog.rs:309-316); xtask/src/invariants.rs:452-484 omits schedule/pending.rs from its sidebar-graph list. |
| 2026-09-10 | pass-15a module `store/snapshot` | `crates/rimz/src/{store,diag.rs,diag}`; `crates/rimz/tests/integration/{store,performance/fold_incremental.rs}`; `xtask/src/invariants.rs`; `docs`; `refactor-target.toml` | `c4789306c` | deepen ×2; delete three transport structs, four pass-throughs, stale-owner branch, CardAdmission and two accessors; narrow ×6 | −108 (18101→17993) | snapshot 63→62; store 343→342 | −3 internal (258→255); +1 downward (368→369); 0 unranked (12→12) | Contract `/tmp/pass-15a/contract.toml` passes with complete evidence: test SLOC +4 (27831→27835), Rust files +0, production ceiling −100 and snapshot esc ceiling 62 met. Existing pins green on base; final target 431 and frozen-caller selection 2206 passed; full gate 6621 passed. No new pin or behavioral change. Source substitutions land apart from the reducer insert operation; empty live-projection edit commit omitted. Kept fold/read façades, distinct lifecycle/launch assemblers and adoption eligibility, quarantine/lifecycle projection, launch index, row constructors, binding dispositions, hook wire and process classification. Deferred: ProcessCard (atlas says private; binary fixture constructs it), pane_agent_kind (atlas says private; frozen sidebar test calls it), lead_unread_row (atlas says pub(crate); frozen CLI test calls it), RuntimeReapInputs.exclude_pane (frozen builders), enrichment sequencing (frozen sidebar/enrich), compute_lazy_agent_pairings wrapper (sideways). Store ↔ diag kept by user decision: a store-side close costs +65 SLOC and duplicates five DiagEvent variants frozen by cli/doctor and sidebar/frame; seam #10 queues the alternative. Advisory: view/reap.rs:12-13 overstates missing-input preservation; no bug fixed. The predicted process.rs:262 sandbox failure did not reproduce. First full gate hit an interrupt timeout and 54.478357ms/50ms mean produce budget; both focused retry and unchanged-tree full gate passed. |
| 2026-09-10 | pass-15c module `daemon_view` + `remote_control` + `room` | `crates/rimz/src/{daemon_view.rs,daemon_view,remote_control.rs,remote_control,room,reload.rs,web/mod.rs,mux/mod.rs,mux/selection.rs,mux/AGENTS.md,cli/supervised/pane.rs,cli/config.rs}`; `crates/rimz/tests/integration`; `docs`; `AGENTS.md`; `refactor-target.toml` | `1dff93f06` | delete ×4, deepen ×2, rehome ×1, narrow ×2 | −85 (5173→5088) | daemon_view 14→11; remote_control 14→12; room 51→48; mux root +1, selection +3 (whole mux 263→264) | −4 internal (133→129); crossing unchanged | Measured with `cargo xtask atlas diff --expect /tmp/scratchpad/pass-15c/contract.toml`: test SLOC +0 (67427), Rust files +0, production ceiling −72 met, all expectations landed with complete evidence. Raw Rust −98, including a verbatim inline test move (+13/−13); raw production −98. Tightened owned ceilings to 11/12/48; room loses the snapshot and both methods, not just the type. Reload→room and remote_control→room each 1→0; CLI pane assembly daemon_view 4→1, remote_control 2→0. Existing pins green on base before structure; final gate 6621 passed, target modules 83 passed, both enable integrations and live tmux repair passed. Deferred: shared prepare/probe helper (+14 SLOC), sidebar_options refresh_ms (+2 to remove), pane_identity_env inherit_channel (four conditional callers), from_resolved sizing (gallery consumes Birth without birth), CLI session-lookup conjunction (notice ordering), and crate-wide workspace-record reads (20+ sites outside scope). Advisories unchanged: cli/room/mod.rs:233,709 repeats the rival guard around config bootstrap; room/birth.rs:288-291 has an incident-pinned broad healthy fallback without a direct unit pin; daemon_view.rs:828 uses the invoking CLI binary for toggle repair rather than birth's staged binary. Loop-lookup debug text, fields and level are unchanged; its tracing target moves from the CLI pane module to daemon_view. |
| 2026-09-10 | pass-15b module `sidebar/refresh` | `crates/rimz/src/sidebar`; `crates/rimz/src/cli/{providers.rs,agents_cmd/refresh.rs,agents_cmd/refresh_context.rs}`; `crates/rimz/tests/integration/{hooks.rs,transcript_watch.rs,performance/enrichment_cadence.rs}`; `docs`; `refactor-target.toml` | `f5e83428d` | collapse usage/credits, rate-limit and PR paths; deepen session/account entries; delete vestigial readers, wrappers and aliases; narrow ×21 | −177 | refresh 79→61; sidebar 361→334 | refresh→harness 8→6; refresh→store 30→29; internal −5; crossing −1 | Contract `/tmp/15b/contract.toml`: production SLOC 15843→15666, tests 24398→24425 (+27), Rust files −2, all six deletes and dependency ceilings landed. Production ceiling −120 exceeded; esc ceiling amended 62→63 to keep the shared cohort schema version (derived Default is version 0), final 61 after deleting two unused facade aliases. Raw Rust −186 splits into production −201 and tests +15, including inline test bodies and cfg(test)-only helpers. Pins first green on c4789306c; final gate 6622 passed; enrichment-cadence performance selection green. Source reviewed at 3594824f0, rebased to fbbe60d34 onto f5e83428d after passes 15a and 15c; all source/test patches unchanged and contract totals reverified. Tightened only sidebar to 334. Deferred: combined live-spend projection (different burst rules, 846f90795/ef1529104); traversal merge (timing-observable); DiffStats/ForcedSessionRefresh narrowing (caller field access); WorktreeRootsCache narrowing (public DiffStatsCache.worktrees emits private_interfaces); shared cohort constant narrowing (schema knowledge stays singular); read_credits_cache deletion (three lines versus eight annotated reads); shared cache-lane helper (21 removed versus 22–32 added, differing stale fallback and publication rules). Kept the private provider and Tea command injection seams for existing effect/argv pins; sessions tests moved whole after exceeding the 500-line inline-module gate. |
| 2026-09-11 | pass-16 seam #10 | `crates/rimz/src/{diag.rs,diag,store,remote,ids.rs,sidebar/notify.rs,sidebar/enrich/tests/mod.rs,sidebar_pane/app/fetch/tests.rs,sidebar_pane/render/tests,cli/sidebar/fixture.rs,cli/doctor/runtime/tests.rs}`; `docs`; `refactor-target.toml` | `555c2dd35` | rehome LinkTier; re-layer diag; close store upward admissions | 0 (20366→20366) | diag 80→80; ids 45→46; store 343→342 | diag→store 1→0; store→diag 4→4; internal 391→392; crossing unchanged | At seam tip a777cb8d6, `cargo xtask gate` passed 6642 tests then `cargo xtask atlas diff --expect /tmp/scratchpad/pass-16/seam.toml --out /tmp/scratchpad/pass-16/seam-proof.md` exited 0 with complete evidence. Test SLOC 43021→43021; Rust files unchanged. Ceiling +2 allowed the forecast +1 (model and fixture imports minus the removed record import), actual 0 after reflow. Atlas counts the removed escaping tier once, not its three definition/re-export tokens. Fifteen existing pins green on the untouched base; enum and pin moved verbatim, all callers import ids without re-exports. First gate hit the unchanged snapshot freshness assertion at store/snapshot/assemble.rs:515; focused retry and unchanged full gate passed. |
| 2026-09-11 | pass-16 module `diag` | `crates/rimz/src/{diag.rs,diag,cli/doctor/runtime.rs,cli/doctor/runtime/tests.rs}`; `docs`; `refactor-target.toml` | `a777cb8d6` | collapse ×1; delete ×1; deepen ×1; narrow ×4 | −18 (3435→3417) | diag 80→76 | internal 19→19; crossing unchanged | `cargo xtask gate` passed 6642 tests then `cargo xtask atlas diff --expect /tmp/scratchpad/pass-16/module.toml --out /tmp/scratchpad/pass-16/module-proof.md` exited 0, complete evidence, ceiling −18 met; test SLOC +2 (2029→2031), Rust files unchanged. The forecast −23 omitted the Admission derive and rustfmt expansion of emit_unlimited; one admission point still removes duplicate limiter/poison/append choreography. Final focused selection 17 passed (including real doctor and reset integrations), diag suite 29 passed, check and doc green. Tightened diag to 76 and store to 342; ids stays 46. As declared, malformed-row debug traces disappear and an unreadable/non-UTF-8 line retains the generation's decoded prefix instead of discarding the entire generation; ordinary JSONL wire and CLI behavior stay unchanged. Deferred: shared rotating-log value (+17), under split (+4), recent deletion (integration reader), kind_name table (new macro); wire payloads and cross-crate test reach refute 35 Atlas narrowings. Final inspect says under and frames_dir_under → pub(crate), record::new → pub(super), log_path, focus_repair::recent, severity and FetchFoldCauseStats → private; kept by verdict for those real callers/wire contracts. NotifyTraceEnvelope → pub(super) and AnomalyKind::key → private are also refuted by library tests. No bugs fixed; focus-repair cap prose and ignored frame-prune removal errors remain advisories. |
| 2026-09-11 | pass-17b module `store/writer` | `crates/rimz/src/store/{writer.rs,writer}`; `crates/rimz/src/cli/{agents_cmd/restart.rs,message/edit.rs}`; `crates/rimz/tests/integration/{message.rs,workspace.rs,store/write_path.rs}`; `docs`; `refactor-target.toml` | `9846073cf` | collapse ×3; delete ×6; narrow ×4 | −115 (3304→3189) | writer 35→31 | crossing downward 81→80; internal 11→10 | Measured with `cargo xtask atlas diff --expect /tmp/scratchpad/pass-17b/contract.toml`: all expectations landed, ceiling −100 met, evidence complete; test SLOC +57 (10699→10756), Rust files unchanged. Raw source −129, tests +64 (queue test files inside src counted as tests). C0 first and green on the original writer; all focused writer, reset, queue, lifecycle, republish and CLI fixture checks green. Empty soft resets keep the existing generation through a fourth Keep policy: the original reseed proposal failed C0. Strangler baseline 3→0; store budget remains 342 because the syntax measure does not see public inherent methods through the private queue module. Kept by verdict: public room-bin entry, reset/rewrite/rotation outcomes, MessageEdit::apply, carryover pruning, and the launch scope without a constructor. Deferred: none. |
| 2026-09-11 | pass-17a module `sidebar/produce` | `crates/rimz/src/sidebar/{produce,AGENTS.md,CLAUDE.md,refresh/mod.rs}`; `crates/rimz/src/sidebar_pane/app/{fetch.rs,fetch/tests.rs,cache_refresh.rs}`; `crates/rimz/src/cli/{sidebar.rs,hooks/binding.rs}`; `crates/rimz/src/testkit/mod.rs`; `crates/rimz/benches/{fleet,hotpath}.rs`; `crates/rimz/tests/integration/{performance,common}`; `docs`; `refactor-target.toml` | `9846073cf` | collapse ×4; delete ×6; narrow ×2 | −90 (6720→6630) | produce 18→16 | crossing same-layer 89→85; downward/unranked unchanged; internal 39→39 | Pin first green on base; final gate 6642 passed, produce 109, fetch 26, integration selection 8; benches link, check/doc pass. Contract `/tmp/scratchpad/pass-17a/contract.toml` exits 0 with ceiling −88, complete evidence, tests 8730→8749 (+19), Rust files −2. Raw source −106, tests +22 (no changed inline tests). Tightened only sidebar 334→332. Kept ProduceErr, ProducedWorkspaceSnapshot, and resolution mux choice by verdict. No bugs fixed. The moved git cache-write warning keeps its message, level and fields; its implicit tracing target follows git::roots → git, with no repository consumer of the old target. |
| 2026-09-11 | pass-18a module `agents` | `crates/rimz/src/agents`; `crates/rimz/src/{remote_control,daemon_view}/tests.rs`; `docs`; `refactor-target.toml` | `0574bc4d4` | delete ×8; narrow ×39 (9 cfg-gated helpers, 22 root aliases, 5 hook items, 3 context/state helpers) | −104 (50249→50145) | agents 972→926 | internal 2184→2184; crossing unchanged | Measured with `cargo xtask atlas diff --expect /tmp/scratchpad/pass-18/agents/contract.toml`: ceiling −80 and esc ceiling 940 met, evidence complete; test SLOC +76 (42431→42507: the gated helpers now count as test code), Rust files unchanged. No pins needed: every item was uncalled, test-only, or an exact visibility change; gate 6643 green. Binary-target tests (`cli/hooks/tests.rs`, `cli/hooks/lifecycle`, `cli/agents_cmd/tests.rs`) reach `update_lifecycle`, `FieldPatch::{as_set,into_set}` and `ProviderAccountBinding::decode`, so those gate on `any(test, feature = "testkit")` after the `AgentState::stub` precedent; `ask_kind`, `json_reply`, `LifecycleSignal::kind`, `LocalTokenPatch::{as_value,into_value}` are `#[cfg(test)] pub(super)`. Of 24 unused root re-exports, 10 deleted outright (canonical `context`/`definition` paths remain), 12 became private `use` (held adapters spell them as `super::X`), 2 kept (`HookRouting`, `OpenAskReadErr` are returned by fns in non-public modules). Dropped by design: moving the six never-overridden `InstallationCapability` defaults onto `AgentDefinition` (41 lines out, 41 in; `Deref` already forwards; the trait default is the contract's home for "not supported"). Tightened agents 972→926. Ran concurrently with 18b/18c (different layers, disjoint paths). No bugs fixed; deferred: none. |
| 2026-09-11 | pass-18b module `sidebar_pane/render` | `crates/rimz/src/sidebar_pane/render`; `docs`; `refactor-target.toml` | `0574bc4d4` | delete ×1; collapse ×6 | −26 (8374→8348) | render 44→43 | internal 258→257; crossing unchanged | Measured with `cargo xtask atlas diff --expect /tmp/scratchpad/pass-18/render/contract.toml`: ceiling −20 and esc ceiling 43 met, evidence complete; test SLOC unchanged, Rust files unchanged; snapshots byte-identical, no `app` file touched; gate 6643 green. Only `draw` and `gauge_percent` were `pub`; the private removals (`append_inert_lines`, `take_cells` folded into `clip`, `agent`, `tab_label`) are proved by `rg`. The `sections` candidate closes as holds: the eleven escaping items the survey called untested each have a consumer pin in `app` or `render/tests`; the repeated width arithmetic is N distinct rules except the trim-then-pad pair, now one. Tightened sidebar_pane 173→172. Advisory, not fixed: the provider tab rail measures `chars().count()`, so a non-ASCII product name mis-sizes its tab (`provider.rs:679,707`); the removed `tab_label` doc promised a capitalization the body never did. Ran concurrently with 18a/18c. Deferred: none. |

Pass-15c cycle dispositions: `daemon_view ↔ remote_control` holds because readiness enters the spec as a DTO while the toggle owns repair; `daemon_view ↔ sidebar` holds because the elder uses the sidebar's disposable published frame and freshness policy while the sidebar reads host presence. `room → reload` remains the admitted durable staging path; only its reverse edge closes.
